apiVersion: v1

kind: Service

metadata:
  name: mycoursework-service
  labels:
    app: mycoursework

spec:
  type: NodePort

  selector:
    app: mycoursework

  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
